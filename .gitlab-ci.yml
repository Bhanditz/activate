
stages:
  - build
  - deploy

build:darwin:
  stage: build
  tags:
    - darwin
  script:
    - scripts/ci-build.sh
  artifacts:
    paths:
      - activate

deploy:
  stage: deploy
  tags:
    - dropsy
  script:
    - scripts/ci-deploy.sh
